---
import Layout from "../../layouts/Layout.astro";
---

<Layout>
    <div class="salon-container">
        <div id="#salonWebsite" class="salon-content">
            <h1 class="salon-title">Salon Website</h1>
            <p class="salon-description">
                The website above was built for my mothers growing salon
                business it show's clients her work as well as providing contact
                and policy information.
            </p>
            <div id="#salonWebsiteLiveDemo" class="salon-video-wrapper">
                <iframe src="https://www.maryheathermelton.com/" frameborder="0"
                ></iframe>
            </div>
            <a class="salon-back-button" href="/proj">Back</a>
        </div>
    </div>
</Layout>

<style>
    .salon-container {
        max-width: 48rem; /* max-w-3xl */
        margin-left: auto;
        margin-right: auto;
        padding-left: 1rem; /* px-4 */
        padding-right: 1rem; /* px-4 */
        padding-top: 2rem; /* py-8 */
        padding-bottom: 2rem; /* py-8 */
    }

    .salon-content {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
    }

    .salon-title {
        font-size: 1.5rem; /* text-2xl */
        line-height: 2rem;
        text-align: center;
        font-weight: 700; /* font-bold */
        margin: 1rem; /* m-4 */
        padding: 0.5rem; /* p-2 */
    }

    .salon-description {
        max-width: 36rem; /* max-w-xl */
        text-align: center;
        padding: 0.5rem; /* p-2 */
        margin: 0.5rem; /* m-2 */
    }

    .salon-video-wrapper {
        position: relative;
        padding-bottom: 56.25%; /* pb-[56.25%] for 16:9 aspect ratio */
        height: 400px;
        width: 100%; /* w-full */
        padding: 0.5rem; /* p-2 */
        margin: 0.5rem; /* m-2 */
        margin-top: 1rem; /* my-4 */
        margin-bottom: 1rem; /* my-4 */
    }

    .salon-video-wrapper iframe {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
    }

    .salon-back-button {
        display: inline-block;
        background-color: #3b82f6; /* bg-blue-500 */
        color: #ffffff; /* text-white */
        font-weight: 700; /* font-bold */
        padding-top: 0.5rem; /* py-2 */
        padding-bottom: 0.5rem; /* py-2 */
        padding-left: 1rem; /* px-4 */
        padding-right: 1rem; /* px-4 */
        border-radius: 0.25rem; /* rounded */
        transition: all 0.3s ease-in-out; /* transition duration-300 ease-in-out */
        margin-top: 2rem; /* mt-8 */
        margin-bottom: 1rem; /* mb-4 */
        text-decoration: none;
    }

    .salon-back-button:hover {
        background-color: #1d4ed8; /* hover:bg-blue-700 */
    }
</style>
